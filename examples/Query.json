{
  "type": "Query",
  "fields": {
    "user": [
      {
        "use": "http",
        "options": {
          "url": "$env.BACKEND_URL/v1/user"
        }
      },
      { "use": "camelizeKeys" }
    ]
  }
}
